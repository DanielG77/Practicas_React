 docker run -d --name libretranslate -p 5000:5000 `
>>   -v C:\libretranslate\data:/home/libretranslate/.local/share `
>>   -v C:\libretranslate\cache:/home/libretranslate/.local/cache `
>>   -e LT_LOAD_ONLY=en,es,fr,de,it,pt `
>>   -e LT_UPDATE_MODELS=true `
>>   -e LT_REQ_LIMIT=30 `
>>   -e LT_CHAR_LIMIT=5000 `
>>   --restart unless-stopped libretranslate/libretranslate